TCP_PORT=1337

AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_DEFAULT_REGION=eu-west-1

# Table name is required — the app will refuse to start without it.
DYNAMO_TABLE=gpb-kv-store-local
DYNAMO_ENDPOINT=http://localhost:8000

KMS_ENDPOINT=http://localhost:4566

# KMS_KEY_ARN (Key Management Service Key Amazon Resource Name) is written automatically
# to .localstack.env by 'make up'. For real AWS, set it here pointing at your KMS key.
# KMS_KEY_ARN=arn:aws:kms:eu-west-1:123456789012:key/your-key-id
