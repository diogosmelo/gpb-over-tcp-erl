{application, gpb_over_tcp_erl, [
    {description, "Key-value store over TCP using Protocol Buffers and DynamoDB"},
    {vsn, "0.1.0"},
    {registered, []},
    {mod, {gpb_over_tcp_erl_app, []}},
    {applications, [
        kernel,
        stdlib,
        crypto,     % required by erlcloud for AWS request signing
        inets,      % provides httpc, erlcloud's HTTP client
        erlcloud
    ]},
    {env, []},
    {modules, []},
    {licenses, ["Apache-2.0"]},
    {links, []}
]}.
